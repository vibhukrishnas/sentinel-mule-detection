# Raw data & large caches — NOT needed by the deployed app (regenerate locally)
DataSet.csv
artifacts/X_clean.parquet
artifacts/y.parquet
# Full account matrix — bank data, NOT for the public repo. Upload it live in the
# app to demo all 9,082 accounts; the committed samples/sample_accounts.csv suffices otherwise.
artifacts/all_accounts.parquet
artifacts/all_targets.parquet
# External validation datasets (BAF/AMLSim/IEEE-CIS/ULB/PaySim) — raw archives + extracted.
# Used ONLY for tagged external/graph/auditor experiments, NEVER merged into BOI.
dataset_Sentinel/
# phase run logs
*_run.log
artifacts/**/_run.log
# Python caches
__pycache__/
*.pyc
.ipynb_checkpoints/
# local background-task logs
*.output
.DS_Store
# personal notes & provided input docs (kept locally, not in the repo)
memory.md
Topic.pdf
BOI hackathon manual.pdf
# agent / editor internals (not part of the solution)
.claude/
.vscode/
